[project]
name = "irodori-tts"
version = "0.1.0"
description = "Irodori-TTS training and inference codebase"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12, <3.13"
dependencies = [
    "dacvae",
    "datasets>=3.0.0",
    "gradio>=5.0.0",
    "huggingface-hub>=0.34.0",
    "llvmlite>=0.40.0",
    "numba>=0.57.0",
    "peft>=0.18.0",
    "pyyaml>=6.0",
    "safetensors>=0.7.0",
    "soundfile>=0.12.0",
    "sentencepiece>=0.2.0",
    "silentcipher @ git+https://github.com/SesameAILabs/silentcipher.git@d46d7d0893a583d8968ab3a6626e2289faec9152",
    "torch>=2.10.0",
    "torchaudio>=2.10.0",
    "torchcodec>=0.10.0,<0.11.0",
    "transformers>=5.12.1,<6",
    "tqdm>=4.67.3",
    "wandb>=0.17.0",
    "torchdata>=0.11.0",
]

[project.optional-dependencies]
cpu = [
    "torch>=2.10.0,<2.11.0",
    "torchao>=0.16.0,<0.17.0",
    "torchaudio>=2.10.0,<2.11.0",
    "torchcodec>=0.10.0,<0.11.0",
]
cu128 = [
    "torch>=2.10.0,<2.11.0",
    "torchao>=0.16.0,<0.17.0",
    "torchaudio>=2.10.0,<2.11.0",
    "torchcodec>=0.10.0,<0.11.0",
]
# Upstream Linux ROCm (explicitly marked for Linux only)
rocm = [
    "torch>=2.10.0,<2.11.0; sys_platform == 'linux'",
    "torchaudio>=2.10.0,<2.11.0; sys_platform == 'linux'",
    "pytorch-triton-rocm>=3.5.1; sys_platform == 'linux'",
    "triton-rocm>=3.6.0; sys_platform == 'linux'",
]
xpu = [
    "torch>=2.10.0,<2.11.0; sys_platform == 'linux' or sys_platform == 'win32'",
    "torchao>=0.16.0,<0.17.0; sys_platform == 'linux' or sys_platform == 'win32'",
    "torchaudio>=2.10.0,<2.11.0; sys_platform == 'linux' or sys_platform == 'win32'",
    "torchcodec>=0.10.0,<0.11.0; sys_platform == 'linux' or sys_platform == 'win32'",
    "triton-xpu==3.6.0; sys_platform == 'linux' or sys_platform == 'win32'",
]
# Windows native AMD ROCm (RDNA2: RX 6000 series, gfx103X)
rocm-win = [
    "torch>=2.10.0,<2.11.0",
    "torchaudio>=2.10.0,<2.11.0",
    "rocm",
    "rocm-sdk-core",
    "rocm-sdk-devel",
    "rocm-sdk-libraries-gfx103x-all",
]
# Windows native AMD ROCm (RDNA3: RX 7000 series, gfx110X)
rocm-win-rdna3 = [
    "torch>=2.10.0,<2.11.0",
    "torchaudio>=2.10.0,<2.11.0",
    "rocm",
    "rocm-sdk-core",
    "rocm-sdk-devel",
    "rocm-sdk-libraries-gfx110x-all",
]
dev = [
    "ruff>=0.9.0",
]

[tool.uv]
environments = [
    "sys_platform == 'win32'",
]
conflicts = [
    [
        { extra = "cpu" },
        { extra = "cu128" },
        { extra = "rocm" },
        { extra = "xpu" },
        { extra = "rocm-win" },
        { extra = "rocm-win-rdna3" },
    ],
]

[tool.uv.sources]
torch = [
    { index = "pytorch-cpu", extra = "cpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
    { index = "pytorch-cu128", extra = "cu128" },
    { index = "pytorch-rocm", extra = "rocm", marker = "sys_platform == 'linux'" },
    { index = "pytorch-rocm-win", extra = "rocm-win", marker = "sys_platform == 'win32'" },
    { index = "pytorch-rocm-win-rdna3", extra = "rocm-win-rdna3", marker = "sys_platform == 'win32'" },
]
torchaudio = [
    { index = "pytorch-cpu", extra = "cpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
    { index = "pytorch-cu128", extra = "cu128" },
    { index = "pytorch-rocm", extra = "rocm", marker = "sys_platform == 'linux'" },
    { index = "pytorch-rocm-win", extra = "rocm-win", marker = "sys_platform == 'win32'" },
    { index = "pytorch-rocm-win-rdna3", extra = "rocm-win-rdna3", marker = "sys_platform == 'win32'" },
]
pytorch-triton-rocm = [
    { index = "pytorch-rocm", extra = "rocm", marker = "sys_platform == 'linux'" },
]
triton-rocm = [
    { index = "pytorch-rocm", extra = "rocm", marker = "sys_platform == 'linux'" },
]
triton-xpu = [
    { index = "pytorch-xpu", extra = "xpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
]
dacvae = { git = "https://github.com/facebookresearch/dacvae" }
rocm = [
    { index = "pytorch-rocm-win", extra = "rocm-win", marker = "sys_platform == 'win32'" },
    { index = "pytorch-rocm-win-rdna3", extra = "rocm-win-rdna3", marker = "sys_platform == 'win32'" },
]
rocm-sdk-core = [
    { index = "pytorch-rocm-win", extra = "rocm-win", marker = "sys_platform == 'win32'" },
    { index = "pytorch-rocm-win-rdna3", extra = "rocm-win-rdna3", marker = "sys_platform == 'win32'" },
]
rocm-sdk-devel = [
    { index = "pytorch-rocm-win", extra = "rocm-win", marker = "sys_platform == 'win32'" },
    { index = "pytorch-rocm-win-rdna3", extra = "rocm-win-rdna3", marker = "sys_platform == 'win32'" },
]
rocm-sdk-libraries-gfx103x-all = [
    { index = "pytorch-rocm-win", extra = "rocm-win", marker = "sys_platform == 'win32'" },
]
rocm-sdk-libraries-gfx110x-all = [
    { index = "pytorch-rocm-win-rdna3", extra = "rocm-win-rdna3", marker = "sys_platform == 'win32'" },
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[[tool.uv.index]]
name = "pytorch-rocm"
url = "https://download.pytorch.org/whl/rocm7.1"
explicit = true

[[tool.uv.index]]
name = "pytorch-xpu"
url = "https://download.pytorch.org/whl/xpu"
explicit = true

[[tool.uv.index]]
name = "pytorch-rocm-win"
url = "https://rocm.nightlies.amd.com/v2-staging/gfx103X-all/"
explicit = true

[[tool.uv.index]]
name = "pytorch-rocm-win-rdna3"
url = "https://rocm.nightlies.amd.com/v2-staging/gfx110X-all/"
explicit = true

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",
    "W",
    "F",
    "I",
    "B",
    "C4",
    "UP",
]
ignore = [
    "E501",
    "B008",
    "C901",
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]

[tool.ruff.lint.isort]
known-first-party = ["irodori_tts"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.setuptools]
packages = ["irodori_tts"]
